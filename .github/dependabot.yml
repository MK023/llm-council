# Le uniche dipendenze del progetto sono le GitHub Actions stesse (stdlib-only altrove).
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
